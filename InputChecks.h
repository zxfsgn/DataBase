#pragma once
// #include "Student.h"

// isStudentsEmpty(Student* students, int& studentsAmount);
int GetCorrectValue();
int BirthCheck(int, int);
bool IndexCheck(int index, int studentsAmount);
bool isStringValid(wchar_t *str);
int GradeCheck(int gradeNumber);
